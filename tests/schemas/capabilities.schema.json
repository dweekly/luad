{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "CapabilityManifest",
  "description": "Global tool capability manifest serialized across text and JSON interfaces.",
  "type": "object",
  "required": [
    "diagnostic_catalog",
    "dialects",
    "evidence",
    "experimental_dialects",
    "export",
    "planned_dialects",
    "schema_version",
    "supported_dialects",
    "tool_name",
    "tool_version"
  ],
  "properties": {
    "diagnostic_catalog": {
      "description": "Public diagnostic-catalog discovery entry point.",
      "allOf": [
        {
          "$ref": "#/definitions/DiagnosticCatalogCapability"
        }
      ]
    },
    "dialects": {
      "description": "Full capabilities by dialect.",
      "type": "array",
      "items": {
        "$ref": "#/definitions/DialectCapability"
      }
    },
    "evidence": {
      "description": "Verification and evidence claims.",
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "experimental_dialects": {
      "description": "List of dialect identifiers currently in `experimental` tier.",
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "export": {
      "description": "Public batch-export discovery entry point.",
      "allOf": [
        {
          "$ref": "#/definitions/ExportCapability"
        }
      ]
    },
    "planned_dialects": {
      "description": "List of dialect identifiers currently in `planned` tier.",
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "schema_version": {
      "description": "Schema major version.",
      "type": "integer",
      "format": "uint32",
      "minimum": 0.0
    },
    "supported_dialects": {
      "description": "List of dialect identifiers currently in `supported` tier (empty until full release gates pass).",
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "tool_name": {
      "description": "Tool name.",
      "type": "string"
    },
    "tool_version": {
      "description": "Tool semver version.",
      "type": "string"
    }
  },
  "definitions": {
    "DiagnosticCatalogCapability": {
      "description": "Machine-discoverable entry point for the public diagnostic catalog.",
      "type": "object",
      "required": [
        "command",
        "formats",
        "schema"
      ],
      "properties": {
        "command": {
          "description": "CLI command that lists or looks up diagnostic descriptors.",
          "type": "string"
        },
        "formats": {
          "description": "Output formats supported by the command.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "schema": {
          "description": "Schema name accepted by `luad schema`.",
          "type": "string"
        }
      }
    },
    "DialectCapability": {
      "description": "Detailed metadata for a supported or planned bytecode dialect.",
      "type": "object",
      "required": [
        "completed_gates",
        "display_name",
        "evidence",
        "features",
        "id",
        "opcode_count",
        "required_gates",
        "status"
      ],
      "properties": {
        "completed_gates": {
          "description": "Completed and verified evidence gates.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "display_name": {
          "description": "Human-readable title and version span.",
          "type": "string"
        },
        "evidence": {
          "description": "Provenance evidence backing the dialect claims.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "features": {
          "description": "Feature capabilities supported by luad for this dialect.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "id": {
          "description": "Canonical identifier (e.g. \"lua5.4\").",
          "type": "string"
        },
        "opcode_count": {
          "description": "Number of verified opcodes in the instruction set table.",
          "type": "integer",
          "format": "uint",
          "minimum": 0.0
        },
        "required_gates": {
          "description": "Required evidence gates needed before becoming `supported`.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "status": {
          "description": "Support tier: planned, experimental, supported.",
          "allOf": [
            {
              "$ref": "#/definitions/SupportTier"
            }
          ]
        }
      }
    },
    "ExportCapability": {
      "description": "Machine-discoverable entry point for the export interface.",
      "type": "object",
      "required": [
        "command",
        "fact_families",
        "formats",
        "link_conventions",
        "schema"
      ],
      "properties": {
        "command": {
          "description": "CLI command that runs batch export.",
          "type": "string"
        },
        "fact_families": {
          "description": "Selectable counted fact families supported by `--facts`.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "formats": {
          "description": "Output formats supported by the command.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "link_conventions": {
          "description": "Optional linking conventions supported by `--link-convention`.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "schema": {
          "description": "Schema name accepted by `luad schema`.",
          "type": "string"
        }
      }
    },
    "SupportTier": {
      "description": "Support tier for a dialect or feature.",
      "oneOf": [
        {
          "description": "Dialect is planned on roadmap but not yet implemented.",
          "type": "string",
          "enum": [
            "planned"
          ]
        },
        {
          "description": "Dialect implementation is present but proof gates are incomplete.",
          "type": "string",
          "enum": [
            "experimental"
          ]
        },
        {
          "description": "All required evidence and differential oracle gates pass.",
          "type": "string",
          "enum": [
            "supported"
          ]
        }
      ]
    }
  }
}
